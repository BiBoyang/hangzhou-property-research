# 我用 AI 审了 AI 抽的数据：185 条指标的人审流水线与四种抽取模式错误

上一篇讲了怎么用 61 份投行研报搭本地 RAG。当时埋了个雷：指标库是 LLM 从研报里抽出来的 3369 条结构化数据（后来去重到 1477 条），入库前过了程序断言——"每条指标的原文摘录必须是源文件逐字子串，且数字本身出现在摘录里"。

这条断言能防"编造"，但防不了"抽错"。摘录确实在原文里，数字也确实在摘录里——可这个数字是**累计值还是单月值**、是**成交量还是价格**、是**城市整体还是其中一个价格段**，断言一概不管。

这篇讲我怎么给这 1477 条指标做人审：抽样 185 条，让一个 flash 模型预审，我再终审。错误率 4.9%，四种错误模式，外加一次我和 AI 一起翻车的自纠。

## 一、流水线：机器把 185 收敛到 7，人只干需要翻原文的活

**第一步：分层随机抽样。** 全库 1477 条按来源文件分层（52 个文件每个至少 2 条，总量 13%），随机种子固定可复现。

**第二步：flash 模型预审。** 规则只有一条核心："你只判断记录是否忠实于摘录，不判断摘录是否符合现实。"每条记录只认领一个数值，摘录里其他数字是隔壁记录的。输出一行一条 JSON：pass / fail / suspect + 错误类型 + evidence（必须是摘录逐字子串）。温度设 0，每批跑两遍，只采信一致结果——这是用弱模型的标准手法。

**第三步：按根因分组。** flash 判完自动把 suspect 按"同一类问题"分组：月度网格缺表头、榜单取列不一致、HK 仪表盘列义不明……

**第四步：人工终审，按组处理而不是按条处理。** 每组只需要干一件事：去原文把**表头**翻出来。PDF 表格被解析成无表头的数据行，判断"这列是不是 2 月单月"的唯一办法是回到原文找到表头——`grep` 那个数字，表头就在上下三行里，30 秒一组。

这个漏斗的真实效果是：185 条 → flash 标出 81 pass / 98 suspect / 6 fail → 我实际需要翻原文的只有二十来组。纯人工审 185 条要两三个小时，这套流程 40 分钟。

## 二、四种错误模式（全是教程里不会写的）

185 条里的真错误全部集中在表格类摘录，散文段落几乎零失误。四种模式：

**模式 1：累计列和单月列取错。** JPM 的房企销售榜第一数值列是"1-2 月累计"、第二列才是"2 月单月"；而 UBS 的榜单第一列就是单月。抽取器不看表头凭位置取数，JPM 那张表一半的房企把累计当成了单月。华润置地 2 月单月 70 亿，库里躺着 150 亿。

**模式 2：不等式被压成点值。** "整体租金回报率仍 <2%"变成 2.0；"成交超 3 万套"变成 30000；"预计下行空间 <9%"变成 -9.0。方向没错，但不等号里的信息没了——"<2%"和"2.0%"是两种市场。

**模式 3：分布表的行被当成城市整体。** 冰山指数有张表按总价段（0-2mn、2-3mn……7-10mn）列各价格段的环比。抽取器把"7-10mn 档环比 +0.4%"记成了"上海全市环比 +0.4%"。行标签是维度值的时候，那一行的数字只属于那个维度。

**模式 4（最疼的一个）：量与价记进同一个字段。** 指标 schema 里"yoy_change_pct"是通用的，没有单独的"成交量同比"。于是香港二手成交量同比 +288%、内地 24 城成交量同比 +64% 全进了价格同比字段。谁查"上海二手价格同比"都会拿到 +64%——这已经不是误差，是错误信息。

模式 4 还带来一个连锁事故：我的市场阶段评估器一直在报"杭州价格信号方向冲突，JPM 说 -6.6%、MS 说 +31%"。查下来 MS 那个 +31% 就是 24 城网格里杭州的**成交量**同比。所谓"机构分歧"，一半是数据错配。**跨机构数字打架，先查口径和数据质量，再谈市场分歧。**

## 三、自纠：我和 flash 一起误判的那次

必须写这段，因为它打的是整个流水线的脸。

flash 在 JPM 房企销售文件里发现"取列不一致"：一部分记录取第一列、一部分取第二列。我复核了表头，确认第一列是累计，于是判定"取第一列的记录全部取错，整文件重抽"。

改到一半我发现这个文件有个 `note` 字段：每条记录都标着"2M26累计权益签约销售额"或"2026年2月单月权益签约销售额"——**这个文件本来就成对存放累计和单月两条记录**，我们判"取错"的那些，恰恰是标注正确的累计行。

flash 漏看了 note 字段，我复核时也只看了表头没看 note。全部误改回滚，只保留一处真修。

教训有两层：① 记录不等于 quote，note/单位/口径标记都是记录的一部分；② **终审方法本身也需要被审**——我设计的"翻表头"终审法，在这个文件上系统性失效，因为它的正确性依赖"记录只有 quote 和值"这个假设。和上一篇那句"验收集本身也需要被评测"是同一个道理：任何过滤器都有自己的盲区，包括人。

## 四、修复：改 schema，不只是改数据

只修数据不修 schema，重抽一次就复发。这次做了三件事：

1. **schema 量价分家**：新增 secondary_volume_yoy_pct / new_home_volume_yoy_pct / total_volume_yoy_pct / price_change_from_peak_pct，yoy_change_pct 从此价格专用。成交量各归各位，查询层不用改一行代码，污染自然消退。
2. **抽取提示词加表格四规则**：累计与单月必须按表头、不等式进 value_text、维度行不升级整体、量价分字段。
3. **series 渠道文件**：中指院月度报告一城一文件，价格序列永远单点。新建一个稳定的月度渠道文件承载城市序列表，和外部数据走同一个模式。

## 五、结果

- 185 条抽样：真错 9 条单点 + 3 个文件重点修复，错误率 4.9%（2%-5% 区间：流程要修，但不是灾难）
- 修完跑 25 题验收集：**25/25 全过，系统建成以来首次**
- 市场阶段评估翻转：杭州从"底部震荡"变成"持续下行"——不是市场变了，是数据干净了。成交量不再给价格信号打鸡血之后，评估器看到的是真实行情

最后说句实话：抽样 13% 就意味着没抽到的 87% 里可能还有漏网。但错误是模式化的，模式修了，漏网的是个位数而不是系统性污染。抽样人审的价值从来不是抓光所有的虫，是**抓到模式**。

---

*相关：上一篇《我用 61 份投行研报搭了个本地 RAG：从向量检索失灵到双层架构》。系统代码开源（GitHub: BiBoyang/hangzhou-property-research），人审抽样清单、终审记录与修正 diff 均可追溯。*
